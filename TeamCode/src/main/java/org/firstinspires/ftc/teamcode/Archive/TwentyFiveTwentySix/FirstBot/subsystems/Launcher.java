package org.firstinspires.ftc.teamcode.Archive.TwentyFiveTwentySix.FirstBot.subsystems;

//import com.arcrobotics.ftclib.command.SubsystemBase;
//import com.arcrobotics.ftclib.hardware.SimpleServo;
//import com.arcrobotics.ftclib.hardware.motors.CRServo;
//import com.arcrobotics.ftclib.hardware.motors.Motor;
//import com.qualcomm.robotcore.eventloop.opmode.Disabled;
//import com.qualcomm.robotcore.hardware.HardwareMap;
//@Disabled

public class Launcher /*extends SubsystemBase*/{
    /*private Motor outtakeLeft;
    private Motor outtakeRight;
    private boolean on = false;

    public Launcher(HardwareMap hardwareMap, String leftMotorID, String rightMotorID, boolean leftReverse, boolean rightReverse) {
        outtakeLeft = new Motor(hardwareMap, leftMotorID);
        outtakeRight = new Motor(hardwareMap, rightMotorID);

        outtakeLeft.setInverted(leftReverse);
        outtakeRight.setInverted(rightReverse);

        outtakeLeft.setRunMode(Motor.RunMode.RawPower);
    }

    public void toggle() {
        if(!on) {
            outtakeLeft.set(0.65);
            outtakeRight.set(0.65);
            on = true;
        }
        else {
            outtakeLeft.set(0);
            outtakeRight.set(0);
            on = false;
        }
    }*/
}
