package org.firstinspires.ftc.teamcode.Archive.TwentyFiveTwentySix.FirstBot.subsystems;
/*
import com.arcrobotics.ftclib.command.SubsystemBase;
import com.arcrobotics.ftclib.hardware.SimpleServo;
import com.arcrobotics.ftclib.hardware.motors.CRServo;
import com.arcrobotics.ftclib.hardware.motors.Motor;
import com.qualcomm.robotcore.eventloop.opmode.Disabled;
import com.qualcomm.robotcore.hardware.HardwareMap;
@Disabled
*/
public class Lifter /*extends SubsystemBase*/{
    /*
    private Motor lifter;

    public Lifter(HardwareMap hardwareMap, String lifterID, boolean reverse) {
        lifter = new Motor(hardwareMap, lifterID);

        lifter.setInverted(reverse);
    }

    public void lift(float power) {
        lifter.setRunMode(Motor.RunMode.RawPower);
        lifter.set(power);
    }

    public void liftToPosition(int position) {
        lifter.setRunMode(Motor.RunMode.PositionControl);
        lifter.setTargetPosition(position);
        lifter.set(1);
    }

    @Override
    public void periodic() {

    }*/
}
